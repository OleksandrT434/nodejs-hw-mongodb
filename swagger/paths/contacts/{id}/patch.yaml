tags:
  - Contacts
summary: Update contact by ID
operationId: updateContactById
description: Update a single contact's information using its unique ID.
security:
  - bearerAuth: []
parameters:
  - name: id
    in: path
    required: true
    schema:
      type: string
      description: The unique identifier of the contact.
      example: '64b7f0c2e1d3f2a5b6c8d9e0'
requestBody:
  required: true
  content:
    multipart/form-data:
      schema:
        type: object
        description: Provide any of the contact fields to update (at least one).
        minProperties: 1
        properties:
          name:
            type: string
            description: The name of the contact.
            example: John Doe
          phoneNumber:
            type: string
            description: The phone number of the contact.
            example: '+380991234567'
          email:
            type: string
            format: email
            description: The email address of the contact.
            example: test.example@mail.com
          contactType:
            type: string
            description: The type of contact (e.g., personal, work).
            enum: [personal, work, home]
            example: work
          isFavourite:
            type: boolean
            example: true
            description: Indicates if the contact is marked as a favorite.
          photo:
            type: string
            format: binary
            description: Photo file to upload.

responses:
  200:
    description: Contact updated successfully.
    content:
      application/json:
        schema:
          type: object
          properties:
            id:
              type: string
              example: '64b7f0c2e1d3f2a5b6c8d9e0'
            name:
              type: string
              example: John Doe
            phoneNumber:
              type: string
              example: '+380991234567'
            email:
              type: string
              example: test.Neo@gmail.com
  400:
    $ref: ../../../components/responses/400.yaml
